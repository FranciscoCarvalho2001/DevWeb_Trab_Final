@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">You do not have access to this resource.</p>
</header>

@section Scripts {
    <script>
        var timeoutMilliseconds = 5000; // 5000 milliseconds = 5 seconds

        setTimeout(function () {
            window.location.href = '@Url.Page("./Register")';
        }, timeoutMilliseconds);
    </script>
}
